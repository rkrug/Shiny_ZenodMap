# Resolve effective relation selection from UI state

Resolve effective relation selection from UI state

## Usage

``` r
resolve_relation_selection(selected, available, previous = "All")
```

## Arguments

- selected:

  Current selected relation values.

- available:

  Available relation choices (without \`All\`).

- previous:

  Previously applied selection.

## Value

Character vector of effective selections.
